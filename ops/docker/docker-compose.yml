services:
  woocommerce-mcp:
    build:
      context: ../../
      dockerfile: ops/docker/Dockerfile.http-bridge
    restart: unless-stopped
    environment:
      - PORT=8090
      - ENVIRONMENT=production

networks:
  shared_network:
    external: true
